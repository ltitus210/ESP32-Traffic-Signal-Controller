#pragma once

// A self hosted Access Point mode will start if WiFi fails to connect to an existing AP
#define WIFI_SSID     "YOUR_WIFI_NAME"     // Default SSID to connect to
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD" // Default Password
